<% title "Upload file" %> 
<%= render :partial => "home/breadcrumbs" %> 
  
<%= render 'form' %> 
  
<p> 
	<% if @asset_file.folder %> 
	  <%= link_to "Back to '#{@asset_file.folder.name}' Folder", browse_path(@asset_file.folder) %> 
	<% else %> 
	  <%= link_to "Back", root_url %> 
	<% end %> 
</p>